import Dashboard from "./Dashboard";

/**
 * Index page redirects to Dashboard
 * This is the main entry point of the admin panel
 */
const Index = () => {
  return <Dashboard />;
};

export default Index;
